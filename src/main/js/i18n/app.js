import { defineMessages } from 'react-intl';

const appMessages = defineMessages({
   name: {
      id: 'app.name',
      defaultMessage: 'Dreadball Web Toolkit'
   }
});

export default appMessages;

import { defineMessages } from 'react-intl';

const unitMessages = defineMessages({
   pickPlayer: {
      id: 'label.select.player',
      defaultMessage: 'Pick player'
   }
});

export default unitMessages;

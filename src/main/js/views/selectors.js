
export const selectNavbarVisible = (state) => state.views.navbarVisible;

export const selectSidebarVisible = (state) => state.views.sidebarVisible;

export const selectSmallScreen = (state) => state.views.smallScreen;
